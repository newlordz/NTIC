fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0
psycopg2-binary>=2.9.9
python-dotenv>=1.0.1
httpx>=0.27.0
# Uploads audit-log cold-storage archives to S3 / Cloudflare R2. main.py imports
# this; without it the retention job refuses to delete records (by design) since
# it cannot prove they were durably archived.
boto3>=1.34.0
pytest>=8.0
