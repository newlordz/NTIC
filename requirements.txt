# Root requirements: intentionally a thin pointer to the backend's own list.
#
# These two files used to be maintained separately and DRIFTED. The root copy
# declared bare `uvicorn`, which ships no WebSocket implementation, so any deploy
# path that installed this file (nixpacks.toml, Procfile) silently lost
# /api/ws -- real-time sync returned 404 with "No supported WebSocket library
# detected". It also carried numpy, opencv-python and sqlalchemy, none of which
# are imported anywhere in the codebase (~100 MB of dead weight).
#
# Keep dependencies in NticPlatform.Backend/requirements.txt only.
-r NticPlatform.Backend/requirements.txt
